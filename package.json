{
  "name": "field-app-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild app.jsx --bundle --jsx=automatic --outfile=bundle.js --minify",
    "serve": "python3 -m http.server 8080",
    "test": "node logic.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}